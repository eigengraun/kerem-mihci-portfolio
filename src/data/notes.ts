import { profileData } from "./profile";

export const workspaceNotes = {
  design: {
    titleTR: profileData.notes.titleTR,
    titleEN: profileData.notes.titleEN,
    itemsTR: profileData.notes.itemsTR,
    itemsEN: profileData.notes.itemsEN
  },
  web: {
    titleTR: profileData.notes.titleTR,
    titleEN: profileData.notes.titleEN,
    itemsTR: profileData.notes.itemsTR,
    itemsEN: profileData.notes.itemsEN
  },
  "motion-ai": {
    titleTR: profileData.notes.titleTR,
    titleEN: profileData.notes.titleEN,
    itemsTR: profileData.notes.itemsTR,
    itemsEN: profileData.notes.itemsEN
  }
};
